apiVersion: v1
kind: Pod
metadata:
  labels:
    app: redis
    run: redis
    name: redis-pod
  name: redis
spec:
  containers:
  - image: redis
    name: redis
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
